<div>
<h1>User info</h1>
<%= image_tag @user.get_profile_image(100, 100) %>
<tbody>
  <tr>
    <th>name<th>
    <th><%= @user.name %></th>
  </tr>
  <tr>
    <th>introduction</th>
    <th><%= @user.introduction %></th>
  </tr>
</tbody>
</div>

<div><%= link_to "edit", edit_user_path(@user.id) %></div>

<div>
<% @books.each do |book| %>
<div>
  <table>
    <thead>
      <tr>
        <td></td>
        <td>Title</td>
        <td>Opinion</td>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= image_tag book.user.get_profile_image(100, 100) %></td>
      </tr>
      <tr>
        <td><%= book.title %></td>
      </tr>
      <tr>
        <td><%= book.body %><td>
      </tr>
    </tbody>
  </table>
  <% end %>
</div>

</div>